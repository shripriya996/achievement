@model IEnumerable<achievement.Models.Achievement>

@{
    ViewBag.Title = "Index";
    int k = 0;
}
<link href="~/Content/styles.css" rel="stylesheet">
<!--main-->
<div class="container" id="main">
    <div class="row">
            <div class="col-md-4 col-sm-6">
                <div class="well">
                    <form class="form-horizontal" role="form">
                         <h4>Create New</h4>
                        <p>Give Credit to the achievement by add here</p>
                        <div class="form-group" style="padding:14px;">
                            <button class="btn btn-success pull-right" type="button" onclick="window.location.href='/achievement/create'">Create</button>
                        </div>
                   </form>
                </div>
                @{k = 2;}
                @foreach (var item in Model)
                {
                    if (k % 3 == 0)
                    {
                        <div class="panel panel-default">
                <div class="panel-heading"><span class="pull-right">@Html.ActionLink("Details", "Details", new { id = item.ID })</span> <h4>@Html.DisplayFor(modelItem => item.Name)</h4></div>
                <div class="panel-body">
                    <img src="//placehold.it/150x150" class="img-circle pull-right"> <a href="#">Keyword: Bootstrap</a>
                    <div class="clearfix"></div>
                    <hr>

                    <p>@Html.DisplayFor(modelItem => item.Acheivement)</p>
                </div>
            </div>
                    } k++;
                }
            </div>
            @{k=1;}
        <div class="col-md-4 col-sm-6">
            @foreach (var item in Model)
            {
                if (k % 3 == 0)
                {
                    <div class="panel panel-default">
                        <div class="panel-heading"><span class="pull-right">@Html.ActionLink("Details", "Details", new { id = item.ID })</span> <h4>@Html.DisplayFor(modelItem => item.Name)</h4></div>
                        <div class="panel-body">
                            <img src="//placehold.it/150x150" class="img-circle pull-right"> <a href="#">Keyword: Bootstrap</a>
                            <div class="clearfix"></div>
                            <hr>

                            <p>@Html.DisplayFor(modelItem => item.Acheivement)</p>

                        </div>
                    </div>
                } k++;
            }
        </div>
        @{k=0;}
        <div class="col-md-4 col-sm-6">
            @foreach (var item in Model)
            {
                if (k % 3 == 0)
                {
                    <div class="panel panel-default">
                        <div class="panel-heading"><span class="pull-right"> @Html.ActionLink("Details", "Details", new { id = item.ID })</span> <h4>@Html.DisplayFor(modelItem => item.Name)</h4></div>
                        <div class="panel-body">
                            <img src="//placehold.it/150x150" class="img-circle pull-right"> <a href="#">Keyword: Bootstrap</a>
                            <div class="clearfix"></div>
                            <hr>

                            <p>@Html.DisplayFor(modelItem => item.Acheivement)</p>
                        </div>
                    </div>
                } k++;
            }
        </div>

    <div id="products"></div>
    </div><!--/row-->
</div><!--/main-->